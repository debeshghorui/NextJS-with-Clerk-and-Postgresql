import type { NextConfig } from 'next';

const nextConfig: NextConfig = {/* config options here */};

module.exports = {
    allowedDevOrigins: ['darkness-yen-expletive.ngrok-free.dev'],
};

export default nextConfig;
